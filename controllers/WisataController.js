/*let Wisata = require("../models/Wisata.js");

let WisataController = {
    find : async(req, res) => {
        let found = await Wisata.find({nama_wisata : req.params.nama_wisata});
        res.json(found);
    },
    all : async(req, res) => {
        let allWisata = await Wisata.find()
        res.json(allWisata);
    }
}
module.exports = WisataController;*/